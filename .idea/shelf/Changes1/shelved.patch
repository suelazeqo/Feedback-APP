Index: src/component/FeedbackList.jsx
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>import FeedbackItem from \"./FeedbackItem\";\r\nimport PropTypes from \"prop-types\";\r\n\r\nfunction FeedbackList({feedback, handleDelete}) {\r\n\r\n    if(!feedback || feedback.length === 0){\r\n        return <p>No Feedback Yet</p>\r\n    }\r\n\r\n    return<div className='feedback-item'>\r\n        {feedback.map((item)=> (\r\n        <FeedbackItem\r\n            key={item.id}\r\n            item={item}\r\n            handleDelete={handleDelete}\r\n        />\r\n        ))}\r\n    </div>\r\n}\r\nFeedbackList.propTypes={\r\n    feedback: PropTypes.arrayOf(\r\n        PropTypes.shape({\r\n            id: PropTypes.number.isRequired,\r\n            text: PropTypes.string.isRequired,\r\n            rating: PropTypes.number.isRequired,\r\n        })\r\n    )\r\n}\r\n\r\nexport default FeedbackList\r\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/src/component/FeedbackList.jsx b/src/component/FeedbackList.jsx
--- a/src/component/FeedbackList.jsx	(revision a94619f627eba5b139f866f907994e707c59b927)
+++ b/src/component/FeedbackList.jsx	(date 1740999947351)
@@ -1,30 +1,40 @@
+import { motion, AnimatePresence } from "framer-motion";
 import FeedbackItem from "./FeedbackItem";
 import PropTypes from "prop-types";
 
-function FeedbackList({feedback, handleDelete}) {
-
-    if(!feedback || feedback.length === 0){
-        return <p>No Feedback Yet</p>
+function FeedbackList({ feedback, handleDelete }) {
+    if (!feedback || feedback.length === 0) {
+        return <p>No Feedback Yet</p>;
     }
 
-    return<div className='feedback-item'>
-        {feedback.map((item)=> (
-        <FeedbackItem
-            key={item.id}
-            item={item}
-            handleDelete={handleDelete}
-        />
-        ))}
-    </div>
+    return (
+        <div className="feedback-list">
+            <AnimatePresence>
+                {feedback.map((item) => (
+                    <motion.div
+                        key={item.id} // Keep key only on motion.div
+                        initial={{ opacity: 0, y: -10 }}
+                        animate={{ opacity: 1, y: 0 }}
+                        exit={{ opacity: 0, y: -10 }}
+                        transition={{ duration: 0.3 }}
+                    >
+                        <FeedbackItem item={item} handleDelete={handleDelete} />
+                    </motion.div>
+                ))}
+            </AnimatePresence>
+        </div>
+    );
 }
-FeedbackList.propTypes={
+
+FeedbackList.propTypes = {
     feedback: PropTypes.arrayOf(
         PropTypes.shape({
             id: PropTypes.number.isRequired,
             text: PropTypes.string.isRequired,
             rating: PropTypes.number.isRequired,
         })
-    )
-}
+    ).isRequired, // Make sure this is required
+    handleDelete: PropTypes.func.isRequired, // Ensure this prop is also required
+};
 
-export default FeedbackList
+export default FeedbackList;
